import type { DispatchCenter } from '../../../../model/region';
import { SUD_CITIES } from './cities';

/**
 * CORES SUD dispatch center
 */
export const DC_CORES_SUD: DispatchCenter = {
    id: 'SUD',
    label: 'CORES SUD',
    latitude: 41.9028,
    longitude: 12.4964,
    cities: SUD_CITIES
};
